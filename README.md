# pg-backup
Docker image to backup PostgreSQL into an archive.
Defaults to using its cron entry or can be run manually.

See https://hub.docker.com/repository/docker/peetvandesande/pg-backup/general for information.
